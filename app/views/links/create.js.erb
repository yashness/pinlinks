var append_repo_record = "<%= @append_repo_record %>"
if(append_repo_record){
	$('#' + 'repo_list').prepend("<tr id= <%='repo_record_' + @repo.id.to_s%> ><%= escape_javascript(render(:partial => 'shared/repo_record', :locals => {:repo => @repo ,:links => @repo.links , :user => current_user, :owner => true} )) %></tr>");
}
$('#' + 'link_list').prepend("<%= escape_javascript( render(:partial => 'shared/multiple_link_records', :locals => {:repo => @repo ,:links => @saved_links , :user => current_user ,:owner => true} ) ) %>");
$('#' + 'drop_links_form')[0].reset()
$(".remove_it.to_create_link").click();
$('#' + 'all_links').val("");
$('#message').html("Links in this Repo:");
$($('#send_links_form_group').children()[5]).prepend("<%=escape_javascript(render :partial => 'shared/to_send_links' , :locals => {:links => @saved_links})%>")
$($('#send_repos_form_group').children()[5]).prepend("<%=escape_javascript(render :partial => 'shared/to_send_repos' , :locals => {:repos => [@repo]})%>")
$('.ajax_flash').html('<div class="alert alert-warning alert-dismissable"><button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>"<%= @flash_message%>".</div>')