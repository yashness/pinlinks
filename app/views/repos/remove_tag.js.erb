var id = '<%= @repo_id %>';
$('#' + 'repo_record_' + id).html("<%= escape_javascript(render(:partial => 'shared/repo_record', :locals => {:repo => @repo ,:links => @repo.links , :user => current_user, :owner => true} )) %>")
