<% if object && object.errors.size > 0 %>
  <div id="errorExplanation">
    <h5><%= pluralize(object.errors.size, "error") %> prohibited this record from being saved</h5>
    <ul>
    <% object.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>
