<%if not link.tags.nil? %>
    <%tags = link.tags%>
    <%tags = tags.chomp.split(" ")%>
    <% if not tags.empty?%>
	   <% for tag in tags%>
		   <%=link_to(tag, "/remove_link_tag/#{link.id}/#{tag}" , :class => "link tag")%>
	   <%end%>
   <%end%>
<%end%>