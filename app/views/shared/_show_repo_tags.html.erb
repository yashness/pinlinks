<%if not repo.tags.nil? %>
	<%tags = repo.tags%>
    <%tags = tags.chomp.split(" ")%>
    <% if not tags.empty?%>
	    <% for tag in tags%>
	    <%=link_to(tag, "/remove_repo_tag/#{repo.id}/#{tag}" , :class => "link tag")%>
	    <%end%>
    <%end%>
<%end%>