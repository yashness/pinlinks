$('#send_links_form_group')[0].reset();
$('#send_links_form_group').parent().css({ display: "none" });
$(".remove_it.to_email_link").click();	
$('.jquery-modal.blocker').css({ display: "none" });
$('.ajax_flash').html("<%= escape_javascript(render(:partial => 'shared/flash_message', :locals => {:type => @flash_type ,:flash_message => @flash_message} )) %>");